{% extends 'layout.html.twig' %}

{% block nav_equipodeguardia %}
    {# sin implementar #}
{% endblock %}

{% block nav_sinloguear %}
    {# sin implementar #}
{% endblock %}

{% block contenido %}

    <div class="contenido">
        <div class="panel-principal">
            <form class="formulario-principal" action="#" id="formulario">
                <h1>{{usuario.nombre}} {{usuario.apellido}}</h1>
                <div class="label-input">
                    <label>Nombre:</label>
                    <input class="inputf" type="text" value={{usuario.nombre}}>
                </div>
                <div class="label-input">
                    <label>Apellido:</label>
                    <input class="inputf" type="text" value={{usuario.apellido}}>
                </div>
                <div class="label-input">
                    <label>Email:</label>
                    <input class="inputf" type="text" value={{usuario.Email}}>
                </div>
                <div class="label-input">
                    <label>Usuario bloqueado:</label>
                    <input class="inputf checkb" type="checkbox">
                </div>
                <h2>Seleccione uno o más roles</h2>
                {% for rol in todoslosroles %}
                    <div class="label-input">
                        <label>{{rol}}</label>
                        <input class="inputf checkb" type="checkbox"
                    {% for roluser in usuario.roles %}
                        {% if rol == roluser %}
                            checked
                        {% endif %}
                    {% endfor %}
                    >
                    </div>
                {% endfor %}
            </form>
            <button type="submit" form="formulario" class="boton-panel-principal">Crear usuario</button>

        </div>

    </div>
{% endblock %}